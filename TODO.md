# IMSA 待辦技術與業務演進清單 (TODO Backlog)

本文件彙整 IMSA 系統在核心業務狀態機、資安與帳號防禦上待推進的優化項目，作為後續迭代之基準。

---

## 守護業務與狀態機缺陷 (Safety & State Machine)

- [ ] **警報持續追蹤與升級機制 (Alert Escalation)**
  - **問題**：使用者一旦超過 12 小時未打卡變為 `ALERTED` 後，下一輪排程因狀態已非 `SAFE` 而不再處理，導致若使用者持續昏迷，系統僅通報一次後陷入沉默。
  - **規劃**：
    - 在 `users` 表新增 `last_alerted_at`（上次通報時間）與 `alert_level`（警報等級）。
    - 排程器若發現使用者仍處於 `ALERTED` 且超過冷卻時間（如每 6 小時），再次觸發通報或提高通報層級。

- [ ] **9 小時 WARNING 預警狀態觸發 (Missing Warning State)**
  - **問題**：`SafetyStatus` 定義了 `WARNING`，但目前全系統從未將使用者標記為 `WARNING`，直接從 `SAFE` 跳到 `ALERTED`，缺少事前提醒。
  - **規劃**：
    - 排程器增加區間判定：超過 9 小時未打卡但未滿 12 小時者，標記為 `WARNING`。
    - 僅發送 App 本地或推播通知提醒本人打卡，不打擾緊急聯絡人。

- [ ] **緊急聯絡人校驗與防空防護 (Emergency Contact Validation)**
  - **問題**：目前緊急聯絡人為選填，若使用者未填寫，逾期時無任何親友可收簡訊，失去守護效益。
  - **規劃**：
    - 註冊流程或首次啟動守護前強制要求設定至少一位有效緊急聯絡人。
    - 增加緊急聯絡人確認簡訊（雙向確認），確保號碼非虛構。

- [ ] **使用者自訂守護安全週期 (Custom Safe Window)**
  - **問題**：系統寫死 12 小時為逾期門檻，無法滿足高風險長者（如 6~8 小時）或特定生活型態族群的需求。
  - **規劃**：
    - 在 `User` 實體新增 `safe_window_hours`（預設 12，允許設定 4~24 小時）。
    - 排程器條件改為動態依據每位使用者的週期進行比對。

- [ ] **手機端 11 小時平安預警卡片彈出前的「最後防線 Double Check」**
  - **問題**：當硬體 RTC 鬧鐘到達觸發 `PreAlertNotificationReceiver` 時，若期間曾有互動但未成功推遲鬧鐘，卡片會直接彈出打擾。
  - **規劃**：
    - 應用上線時於 `PreAlertNotificationReceiver.onReceive` 增加最後一道校驗：檢查手機近期點亮與互動日誌（如 UsageStatsManager 或本地最近解鎖戳記）。
    - 若判定近期（如過去數小時內）其實曾有互動，則自動靜默取消卡片並重新預約下一輪預警，防止誤報。

---

## 資安與帳號防禦邊界 (Security & Account Protection)

- [ ] **API 存取權限與 JWT 身分驗證 (JWT Auth & IDOR Protection)**
  - **問題**：目前所有 API 皆為 `permitAll()`，若已知使用者的 UUID，任何人皆可查詢個資、變更密碼或替他人打卡（水平越權）。
  - **規劃**：
    - 引入 Spring Security + JWT 驗證機制。
    - 使用者登入成功後核發 Access Token / Refresh Token。
    - 端點校驗 Token 中的 User ID 與路徑參數是否匹配，杜絕越權操作。

- [ ] **登入防暴力破解與頻率限制 (Brute Force Protection)**
  - **問題**：登入失敗時僅回傳錯誤，未限制嘗試頻率，易遭受字典檔暴力攻擊。
  - **規劃**：
    - 實作登入失敗計數器（可基於記憶體 Caffeine 或 Redis）。
    - 同一門號或 IP 連續失敗 5 次，鎖定登入 15 分鐘，或要求驗證碼。

- [ ] **帳號註銷與軟刪除機制 (Soft Delete)**
  - **問題**：目前無刪除帳號端點，若直接硬刪除 User 會因外鍵約束報錯，或造成歷史打卡安全日誌全部灰飛煙滅。
  - **規劃**：
    - 在 `UserStatus` 增加 `DELETED`（已註銷）。
    - 註銷時將個人隱私資訊去識別化，保留歷史打卡日誌備查，並停用排程檢查與登入。
